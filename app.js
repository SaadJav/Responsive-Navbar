 let menuIcon = document.querySelector(".humburger")

        menuIcon.addEventListener("click", () => {
            let menuBtn = document.querySelector(".navmenu");
            menuBtn.classList.toggle("active")
        })
